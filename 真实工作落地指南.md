# 资金台账 AI 自动化系统 · 真实工作落地指南

> 适用场景：你正式入职财务/会计岗后，把这套系统用在实际账务上。
> 配套代码：`资金台账AI自动化项目/src/`（已跑通，演示数据 97 条、分类 100%、预警 11 条）。
> 本文档说的"你"，指实际操作的会计本人。

---

## 0. 先说三句大实话（边界，别跳过）

1. **这是辅助工具，不是认证会计系统。** AI 分类给出的是"建议科目"，最终账簿责任在你（会计）身上。自动化提高效率，不能替代你的判断。
2. **真实数据 = 公司机密。** 银行流水、客户名、金额都受《会计档案管理办法》约束，存储、传输、留存都要合规，别随手丢个人 U 盘或公共网盘。
3. **"待分类"和"异常预警"两个表必须人工过一遍。** 系统越用越准，但第一次上线时规则未必覆盖你们公司的真实摘要习惯，别完全放手。

---

## 第 0 步：合规与权限（先做，别直接动数据）

- 找财务负责人/主管确认三件事：
  - 能否用 Python 脚本处理、导出账务数据；
  - 数据允许存在哪（优先公司内网盘 / 财务专用机）；
  - 能否联网装 Python 包（不能就走"离线包/exe"方案，见第三步）。
- 确认公司用什么财务软件（用友 U8 / 金蝶 / 其他）——这决定流水从哪导出、台账往哪接。
- 明确电子台账也要按会计档案留存、可追溯、可打印归档。

---

## 第一步：拿到真实银行流水（数据接入）

**三种数据源，按优先级选：**

| 来源 | 怎么拿 | 适配情况 |
|------|--------|----------|
| 企业网银导出 | 工行/招行/建行企业网银 → 下载 Excel/CSV | ✅ 已适配这三家列名 |
| 财务软件导出 | 用友 U8 / 金蝶 的"银行日记账"导出 | 需把导出列名对一遍 |
| 银企直连 API | 让 IT 给接口，定时拉取 | 进阶，需开发 |

**操作：**
1. 把导出的 CSV/Excel 放进 `data/raw_bank_flows/` 文件夹。
2. 文件名按 `银行名_年份.csv` 命名（如 `工商银行_2026.csv`），和代码里的 `file_map` 对齐。
3. 如果银行列名和演示版不一样，改 `src/data_cleaner.py` 顶部的 `COLUMN_MAP`（每家银行"原始列名 → 标准列名"的映射）。代码已支持多格式日期、带逗号的金额、空值，一般不用动解析逻辑。

> 演示版只做了工行/招行/建行。你们公司用其他银行，照着 `COLUMN_MAP` 加一条即可。

---

## 第二步：校准你的会计科目表（最值钱的一步）

这是分类准不准的关键。**打开 `src/config.py`，改 `CLASSIFICATION_RULES`。**

每条规则长这样：
```python
(["工资", "薪资", "薪酬", "社保"], "应付职工薪酬", "支出", "贷"),
#  ↑关键词列表           ↑你们公司的真实科目名   ↑类别  ↑借贷方向
```

**你要做的：**
- 把"关键词"换成你们公司流水摘要里真实出现的词（如你们叫"发薪"而不是"工资"，就改掉）。
- 把"会计科目"换成你们《会计科目表》里的真实科目（如你们差旅费挂在"销售费用-差旅费"就改过去）。
- 借贷方向以"企业银行存款日记账"视角：收款=借，付款=贷，内部转账看你们核算习惯。
- 大额预警线 `LARGE_AMOUNT_THRESHOLD`（默认 5 万）按你们公司规模调。

> 这一步最花时间但最值钱。规则越贴合你们真实摘要，自动分类命中率越高，你复核的量越少。

---

## 第三步：部署运行环境

**情况 A：公司电脑能联网**
```bash
pip install -r requirements.txt   # 装 pandas / openpyxl / matplotlib
python src/main.py                # 直接跑
```

**情况 B：不能联网 / 同事不懂 Python（推荐给团队用）**
- 我帮你把整个项目用 PyInstaller 打包成 **.exe**，双击就能出台账，不用装 Python、不用联网。
- （这一步需要我额外做，跟我说"打包成 exe"即可。）

**运行后产出：** `output/资金台账_2026.xlsx`，含 7 个 sheet：
`项目说明 / 原始流水 / 标准资金台账 / 资金日报 / 资金月报 / 异常预警 / 可视化看板`。

---

## 第四步：跑系统 + 人工复核闭环（日常核心）

1. 运行 `python src/main.py`（或双击 exe）。
2. 打开 Excel，重点看两张表：
   - **标准资金台账** 的"分类置信度"列 + "收支类别=未识别"的行 → 这些是系统拿不准的，你手动改科目，然后把这条规律补进 `config.py` 的 `CLASSIFICATION_RULES`。
   - **异常预警** → 大额 / 借贷不平 / 疑似重复付款 / 非工作日交易，逐条确认是否真有问题，再决定是否上报主管。
3. **闭环**：你每修正一条，就反哺一条规则 → 系统越用越准，这是它比"一次性脚本"强的地方。

---

## 第五步：对接凭证与汇报

- **归档**：台账 Excel 可直接打印，作为电子会计档案留存（配合系统里的"项目说明"sheet 说明来源）。
- **进财务软件**：台账的"会计科目 + 借贷方向 + 金额"字段结构，已支持批量导入用友 U8 / 金蝶生成凭证（当前演示版未做自动导入，但数据格式就是为此准备的）。
- **周报**：把"异常预警"表每周汇总给主管，体现你主动风控。

---

## 日常节奏建议

- **每月初**：导上月全量流水 → 跑系统 → 复核"未识别"和"异常预警" → 归档 → （可选）导入财务软件生成凭证。
- **每周**：看一眼异常预警，有问题及时报。
- **每半年**：回头优化一次 `CLASSIFICATION_RULES`，把新出现的摘要类型补进去。

---

## 可选增强（要做直接说）

| 增强 | 价值 | 难度 |
|------|------|------|
| 打包 exe / 离线包 | 同事也能用，不依赖 Python 环境 | 低 |
| 接 LLM 做低置信兜底 | 未识别样本用大模型语义分类，命中率再上一个台阶 | 中 |
| 定时任务（每天自动跑） | 不用记得手动跑 | 低 |
| 接数据库替代 Excel | 数据量大、多人协作时更稳 | 中 |
| 自动导入用友 U8 / 金蝶 | 台账直接变凭证，省手动录入 | 中 |

---

## 一句话总结

**真实工作里它不是"一键全自动"，而是"自动清洗分类 + 你把关复核"的半自动流水线**——把最耗时的录入归类活儿交给代码，把判断和责任留在你手里。这恰恰是会计岗用 AI 最稳妥、也最经得起审计追问的姿势。
